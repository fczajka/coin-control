export enum ButtonTypes {
  button = "button",
  submit = "submit",
  reset = "reset",
}

export enum InputTypes {
  text = "text",
  date = "date",
  dateTimeLocal = "datetime-local",
  number = "number",
  password = "password",
}
